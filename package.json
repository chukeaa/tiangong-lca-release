{
  "name": "@tiangong-lca/release",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm run format:check",
    "prepush:gate": "npm run format:check"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
