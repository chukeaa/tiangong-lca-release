{
  "name": "@tiangong-lca/release",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm run format:check",
    "test": "npm --prefix workflows/calculation test && npm --prefix workflows/result-materialization test",
    "prepush:gate": "npm run format:check && npm test"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
