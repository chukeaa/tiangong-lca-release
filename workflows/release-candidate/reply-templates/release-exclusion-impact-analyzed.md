## 🔎 Release 排除影响分析已完成

- 权威影响报告：`{{impactReport}}`
- 报告 SHA-256：`{{impactReportSha256}}`
- 人工审核表为必需：`{{reviewWorkbookRequired}}`

客户端 Agent 将从权威 JSON 生成并逐页检查 Excel 审核表；审核表准备好之前，不请求用户确认排除范围。

下一步：`{{nextActions.0.command}}`
