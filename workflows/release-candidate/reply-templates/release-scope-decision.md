## 🧾 Release 范围决定已记录

- 动作：`{{action}}`
- 决定者：`{{decidedBy}}`
- 决定时间：`{{decidedAt}}`
- 决定目录：`{{scopeDecision}}`
- 决定 SHA-256：`{{scopeDecisionSha256}}`
- 已授权发布：`{{publicationAuthorized}}`

该决定只改变下一次 Candidate 的冻结范围，不修改失败构建或源数据，也不构成批准、上传或发布授权。

下一步：`{{nextActions.0.command}}`
