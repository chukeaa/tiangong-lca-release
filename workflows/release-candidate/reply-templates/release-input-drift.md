# Release 输入发生漂移

```markdown
⚠️ Release Candidate 未生成，因为冻结证据与当前输入字节不一致。

- 命令：`{{command}}`
- 错误代码：`{{error.code}}`
- 原因：{{error.message}}

不要在 Release 阶段修改或绕过这些证据。请返回 Result Materialization，重新确认并冻结输入：

`{{nextActions.0.command}}`
```
