# Calculation Bundle 已下载并校验

```markdown
✅ Package `{{data.packageId}}` 的 Calculation Bundle 已通过数据库 metadata 和 S3 数据面下载到本地并完成完整性校验。

- 本地目录：`{{data.bundleDirectory}}`
- Artifact 数量：`{{data.artifactCount}}`
- 产品文件数量：`{{data.productDownloadCount}}`
- 校验状态：`{{data.verification}}`
- 下载 receipt：`{{data.receiptPath}}`

下一步：

`{{nextActions.0}}`

该过程没有生成或持久化 signed URL，也没有重新触发 Calculation。
```
