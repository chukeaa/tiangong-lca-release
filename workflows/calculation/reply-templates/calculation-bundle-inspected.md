# Calculation Bundle 元数据已读取

```markdown
🔎 已通过参数化只读 SQL 读取 Package `{{data.packageId}}` 的 Calculation Bundle 元数据。

- Bundle：`{{data.bundle}}`
- 查询完整性：`{{completeness.status}}`
- 数据来源：`{{completeness.source}}`
- Signed URL：未生成

下一步：

`{{nextActions.0}}`
```
