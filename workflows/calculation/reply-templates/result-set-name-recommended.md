# ResultSet 名称推荐

```markdown
💡 你还没有提供 ResultSet 名称，我推荐：

`{{error.details.recommendedName}}`

名称由 `ResultSet + 上海时区日期时间` 组成，便于识别，也能降低同一天重复命名的概率。

如果你接受这个名称，我可以执行：

`{{nextActions.0}}`

你也可以提供一个更能表达本次计算目的的名称。
```
