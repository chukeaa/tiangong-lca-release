# Release 数据面环境已同步

```markdown
✅ 已将 workspace `.env` 中缺失的 Release 数据面变量按白名单同步到 Release `.env`。

- 已复制变量名：`{{data.copiedKeys}}`
- 已保留变量名：`{{data.preservedKeys}}`
- workspace 中缺失的变量名：`{{data.missingSourceKeys}}`
- Secret values：未输出

下一步：

`{{nextActions.0}}`
```
