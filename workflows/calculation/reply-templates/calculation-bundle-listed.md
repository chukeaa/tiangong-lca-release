# 可用的 Calculation Bundle 已列出

```markdown
✅ 已通过参数化只读 SQL 列出包含 Calculation Bundle metadata 的 Package。

- 可用 Bundle 数量：`{{completeness.returned}}`
- 列表完整性：`{{completeness.status}}`
- 是否可能还有更多：`{{completeness.mayHaveMore}}`

可用 Bundle：

{{data.items}}

[若存在 warnings] 注意：`{{warnings}}`

下一步请选择一个精确 `packageId`，不会自动采用列表第一项：

`{{nextActions.0}}`

选择 Bundle 时使用精确 `packageId`；列表未调用 Edge Bundle read，也未生成、输出或持久化 signed URL。
```
