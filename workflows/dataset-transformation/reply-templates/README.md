# Dataset Transformation Reply Templates

These templates keep Agent replies aligned with the workflow artifact state. A
`needs_decision` reply is a normal semantic continuation, not a failed command.
