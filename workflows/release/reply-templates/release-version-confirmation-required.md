# 确认 Release 版本号

```markdown
📦 推荐发布版本：`{{recommendedVersion}}`

确认后将生成：

- `{{fileNames.0}}`
- `{{fileNames.1}}`
- `{{fileNames.2}}`
- `{{fileNames.3}}`

请确认是否使用该版本号；也可以回复一个替代版本号。确认前不会构建、上传或发布任何包。
```
