# 🗃️ Elementary Flow 缓存状态

- 缓存路径：`{{cache}}`
- 当前状态：`{{status}}`
- 缓存记录数：`{{recordCount}}`
- 数据库 watermark：`{{databaseWatermark}}`

下一步：

{{nextActions}}

> 常规 Release Intake 只读取有效缓存；缓存缺失或过期时不会隐式下载，刷新必须由用户明确触发。
