# Portal LCIA 投影已撤回并验证

- Projection Publication：`{{projectionPublicationId}}`
- LCIA Publication：`{{lciaResultPublicationId}}`
- Projection Content SHA-256：`{{projectionContentHash}}`
- 结果：`{{disposition}}`
- 请求理由持久化语义：`{{reasonPersistence}}`
- 当前公开：`{{isCurrent}}`
- 通过完整公开可见性谓词：`{{isPubliclyVisible}}`
- Revocation Receipt SHA-256：`{{revocationReceiptSha256}}`
- Receipt：`{{artifacts.revocationReceipt}}`

独立回读已确认 exact projection binding 为 `revoked`，不会再作为当前 Portal LCIA projection 读取。
