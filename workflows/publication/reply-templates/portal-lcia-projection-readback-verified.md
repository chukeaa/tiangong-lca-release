# Portal LCIA 投影独立回读已验证

- Projection Publication：`{{projectionPublicationId}}`
- LCIA Publication：`{{lciaResultPublicationId}}`
- Projection Content SHA-256：`{{projectionContentHash}}`
- Evidence SHA-256：`{{evidenceHash}}`
- 网格：`{{processCount}} × {{impactCount}} = {{valueCount}}`
- 当前公开：`{{isCurrent}}`
- 通过完整公开可见性谓词：`{{isPubliclyVisible}}`
- Verified Event SHA-256：`{{verifiedEventSha256}}`
- Event：`{{artifacts.verifiedEvent}}`

Portal LCIA projection 闭环完成：独立 actor-scoped 回读已验证 exact publication、package、content/evidence hash、行数和 current 状态。
