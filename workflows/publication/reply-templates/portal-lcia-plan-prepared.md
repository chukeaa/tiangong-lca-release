# Portal LCIA 计划已准备

- 命令：`{{command}}`
- 结果：`{{outcome}}`
- 完整性：`{{completeness}}`
- Plan：`{{artifacts}}`
- 下一步：`{{nextActions}}`

这是未授权计划，不表示已经执行远程写入。回复时从 CLI JSON 选择足以识别本次计划的 exact identity 和 confirmation SHA-256，并逐字提供 CLI 返回的下一条命令；不要复制 Plan 中的全部 evidence。
