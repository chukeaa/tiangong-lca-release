# Portal LCIA 投影计划已准备

- Projection：`{{projectionId}}`
- Package：`{{packageId}}` / `{{packageVersion}}`
- LCIA Publication：`{{lciaResultPublicationId}}`
- Package Result SHA-256：`{{packageResultHash}}`
- Projection Content SHA-256：`{{projectionContentHash}}`
- 网格：`{{processCount}} × {{impactCount}} = {{valueCount}}`
- Plan SHA-256：`{{projectionPlanSha256}}`
- 已授权 finalize：`{{projectionFinalizationAuthorized}}`
- Plan：`{{artifacts.projectionPlan}}`

下一步只执行 CLI 返回的 exact-confirm 命令：

`{{nextActions.0.command}}`

当前没有执行远程写入，既有 LCIA publication 和 Candidate Publication 均未改变。
